import { createContext } from "react";

const UserContext = createContext({});
const LoginContext = createContext({});
const UserInfoContext = createContext({});

export { UserContext, LoginContext, UserInfoContext };
